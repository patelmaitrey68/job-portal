# Application Configuration
spring.application.name=Job-Portal
server.port=8080

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/jobportaldb
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=maitrey
spring.datasource.password=12345678
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=3600000

# Error Messages
user.found=User already registered
user.not.found=User is not registered
invalid.credentials=Invalid credentials
sequence.not.found=Unable to get sequence ID for key: {0}

# CORS Configuration
cors.allowed.origins=http://localhost:5173,http://localhost:3000

# Gemini API Key (Replace MOCK with your actual key to use real AI)
gemini.api.key=${GEMINI_API_KEY:MOCK}

# Spring Mail Configuration (disabled for local dev)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=placeholder@gmail.com
spring.mail.password=placeholder
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
